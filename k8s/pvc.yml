apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: db-pvc

spec:
  resources:
    requests:
      storage: 1Gi
  storageClassName: standart
  accessModes:
    - ReadWriteOnce
  selector:
    matchLabels:
      name: db-pvc